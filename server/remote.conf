# This file is the required server configs
# Place it under /etc/rsyslog.d/remote.conf

module(load="imtcp")
input(type="imtcp" port="514")

$template RemoteLogs,"/var/log/remote/%HOSTNAME%/syslog.log"
*.* ?RemoteLogs
& stop
